/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package javaapplication;

/**
 *
 * @author CAMARGO
 */

import java.util.Scanner;
public class Exercicio01 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
       //numero de horas
    Scanner horas = new Scanner(System.in);
System.out.println("Coloque o seu numero de horas: ");
float H = horas.nextFloat();

//Salario de cliente 
    Scanner salario = new Scanner(System.in);
System.out.println("Coloque o seu salario: ");
float S = salario.nextFloat();

//Idade do trabalhador
    Scanner Idade = new Scanner(System.in);
System.out.println("Coloque a sua idade: ");
float idade = Idade.nextFloat();

//Numero de filhos 
    Scanner Filhos = new Scanner(System.in);
System.out.println("Coloque o numero de filhos menores que 14 anos: ");
float filhos = Filhos.nextFloat();

//Tempo de serviço 
    Scanner Tempo_Serviso = new Scanner(System.in);
System.out.println("Coloque a quantidade de tempo no serviso(anos): ");
float tempo_serviso = Tempo_Serviso.nextFloat();

//Bonus por filho
    Scanner Bonus_Filho = new Scanner(System.in);
System.out.println("Coloque a quantidade de bonus por filho: ");
float bonus_filho = Bonus_Filho.nextFloat();

float Salario_Bruto;


//- Calcular o IR ( Imposto de Renda) como segue: 
if(S>1500){
    //- Calcular o IR ( Imposto de Renda) como segue:
    float imposto =((15/100)*S);
    float adicao_familia = (bonus_filho*filhos);
    Salario_Bruto = adicao_familia+(S-imposto);
    System.out.println("Este é o imposto a ser pago: "+imposto);
    
 }
else if(S>500){
    //- Calcular o IR ( Imposto de Renda) como segue:
     float imposto =((8/100)*S);
     float adicao_familia = (bonus_filho*filhos);
     Salario_Bruto = adicao_familia+(S-imposto);
     System.out.println("Este é o imposto a ser pago: "+imposto);
    }
else{
    //- Calcular o IR ( Imposto de Renda) como segue:
    Salario_Bruto = S;
    float imposto = 0;
     float adicao_familia = Salario_Bruto+(bonus_filho*filhos);
    System.out.println("Este é o imposto a ser pago: "+imposto);
        }


//bonus idade
if(idade>40){
    float bonus_idade = S*(2/100);
    Salario_Bruto = bonus_idade+S;
    System.out.println("Salario Bruto: "+Salario_Bruto); 
}
else if(tempo_serviso>15){
    float bonus_serviso = S*(2/100);
    Salario_Bruto = bonus_serviso+S;
    System.out.println("Salario Bruto: "+Salario_Bruto);
}
else if(tempo_serviso<=15|| tempo_servise>5 & idade>30){
    
}

//- Se tempo de serviço for menor ou igual que 15 anos mas superior a 5 anos e idade 
//maior do que 30 anos então ADIC = 1,5% do salario bruto.
//Calcular o salário líquido. Escrever o número do funcionário, o salário bruto, total de
//descontos, adicional e salário líquido








}
}
